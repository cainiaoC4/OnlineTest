#include<iostream>
#include<vector>
//#include <stdio.h>
#include<cmath>
#include<iomanip>
#include<algorithm>
using namespace std;




int main()
{
	/*Base1 base1(1);
	Base2 base2(2);
	Base3 base3;

	fun(&base1);
	fun(&base2);
	fun(&base3);*/
	//Derived obj(1, 2, 3, 4);

	char**strNum = (char**)(new int[5]);
	//char**strNum = static_cast<char**>(new int[5]);
	char** test = new char*[5];

	const char* A = "abdc";
	return 0;
}